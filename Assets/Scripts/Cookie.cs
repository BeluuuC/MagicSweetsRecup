//using UnityEngine;

//public class Cookie : MonoBehaviour
//{
    // Detectar colisiones con el jugador
    //private void OnTriggerEnter2D(Collider2D other)
  //  {
        //if (other.CompareTag("Player"))  // Si el objeto que colisiona es el jugador
    //    {
            // Llamamos al método CollectItem del script Collection
      //      other.GetComponent<Collection>().CollectItem("Cookie");

            // Desactivamos la galleta después de que el jugador la recoja
        //    gameObject.SetActive(false);

            // Aquí podrías agregar un efecto visual o sonido cuando la galleta es recogida
            // Ejemplo:
            // Instantiate(pickupEffect, transform.position, Quaternion.identity);
        //}
    //}
//}
